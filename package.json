{
  "name": "gh-pages",
  "scripts": {
    "dev": "next",
    "build": "next build",
    "start": "next start",
    "start:staging": "SET NODE_ENV=staging next",
    "export": "next export",
    "deploy": "rm -rf node_modules/.cache && rimraf out && next build && next export && touch out/.nojekyll && gh-pages -d out -t true"
  },
  "dependencies": {
    "compression-webpack-plugin": "^4.0.0",
    "dotenv": "^8.2.0",
    "graphql-react": "^11.0.4",
    "next": "latest",
    "next-compose-plugins": "^2.2.0",
    "next-optimized-images": "^2.6.2",
    "react": "^16.13.1",
    "react-dom": "^16.13.1",
    "react-markdown": "^4.3.1",
    "styled-components": "^5.1.1"
  },
  "license": "ISC",
  "devDependencies": {
    "babel-plugin-transform-define": "^1.3.2",
    "gh-pages": "^2.1.1",
    "rimraf": "^3.0.0"
  }
}
